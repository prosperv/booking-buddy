{
    "name": "court-sign-up",
    "version": "0.1.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "start": "npx tsx examples/usage.ts",
        "dev": "tsc -w",
        "test": "vitest",
        "test:watch": "vitest watch",
        "test:coverage": "vitest run --coverage",
        "playwright:install": "npx playwright install chromium"
    },
    "dependencies": {
        "dayjs": "^1.11.21",
        "playwright": "^1.62.1"
    },
    "devDependencies": {
        "@types/node": "^26.1.2",
        "typescript": "^7.0.2",
        "vitest": "^4.1.10"
    },
    "engines": {
        "node": ">=20"
    }
}
